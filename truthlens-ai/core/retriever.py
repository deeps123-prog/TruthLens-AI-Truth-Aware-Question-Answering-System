def retrieve(query: str) -> list:
    # Dummy retrieval (replace with real search later)
    return [
        {"source": "doc1", "content": f"Information related to {query}."},
        {"source": "doc2", "content": f"Additional details about {query}."}
    ]
